# Backup

Owns backup strategy, restore drills, retention windows, backup integrity checks and recovery evidence.

Current canonical paths:

- backup/restore documentation under `docs/`
- persistent runtime/deployment configuration under `deploy/`

Migration rule: backups are incomplete until restore has been tested and documented with evidence.
