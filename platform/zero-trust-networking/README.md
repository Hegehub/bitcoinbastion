# Zero-trust networking

Owns identity-aware network access, least-privilege connectivity, segmentation, mTLS assumptions and internal access boundaries.

Current canonical paths:

- network/deployment manifests under `deploy/`
- security and operations documentation under `docs/`

Migration rule: network access should be explicitly authorized, observable and constrained by service identity where possible.
