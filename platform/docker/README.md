# Docker

Owns container images, Dockerfile conventions, Compose profiles, local runtime packaging and image hardening notes.

Current canonical paths:

- `Dockerfile`
- `docker/`
- Docker Compose files

Migration rule: Docker changes must keep local development, smoke checks and production image expectations aligned.
