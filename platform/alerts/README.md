# Alerts

Owns alert rules, incident notification policy, escalation paths, severity levels and operator-facing incident evidence.

Current canonical paths:

- operations documentation under `docs/`
- alerting/deployment manifests under `deploy/`

Migration rule: alerts must be actionable, deduplicated and tied to a documented runbook or remediation path.
