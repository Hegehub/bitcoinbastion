# Multi-region infra

Owns regional topology, failover design, data placement, latency considerations and sovereignty-aware deployment strategy.

Current canonical paths:

- deployment manifests under `deploy/`
- production architecture documentation under `docs/`

Migration rule: multi-region changes must explicitly document consistency boundaries, failover assumptions and rollback behavior.
