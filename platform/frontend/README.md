# Frontend

Owns user-facing interfaces, web console surfaces, static assets and frontend contract documentation.

Current canonical paths:

- `reflex_frontend/`
- `app/web/`
- `app/api/v1/public.py`

Migration rule: keep UI-facing changes here documented first; move executable code only with matching import, build and test updates.
