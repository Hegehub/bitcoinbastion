# Docs

Owns architecture documentation, runbooks, status reports, truthfulness checks, ADRs and production evidence narratives.

Current canonical paths:

- `docs/`
- documentation validation scripts under `scripts/`

Migration rule: documentation must distinguish implemented behavior from planned, simulated, fallback or future work.
