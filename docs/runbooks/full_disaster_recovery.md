# Full Disaster Recovery Runbook

Run backup verification, restore verification, timeline rebuild, candle rebuild, event rebuild, evidence rebuild, signal rebuild and integrity validation. Recovery is successful only when deterministic replay and integrity are verified.
