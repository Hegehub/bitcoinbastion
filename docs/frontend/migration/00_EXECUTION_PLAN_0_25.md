# Canonical Execution Plan 0–25

> This plan supersedes `00_EXECUTION_PLAN_52_PROMPTS.md` as the active sequencing view. The old file remains historical evidence and must not be used to assign new work.

Every stage preserves no-custody, backend truth, four-state provenance, explicit degraded states, and separate PayRegister boundaries. A stage stops on ambiguous auth/scope/units/timestamps, unsafe serialization, irreversible action, missing owner, new dependency without approval, or a failed prerequisite.

## Stage 1 authority split

Prompt 1 is executed through two non-numbered subgates; Prompts 2–25 and the 69-feature allocation are unchanged:

* **Prompt 1A — Contract Authority Remediation** owns canonical HTTP identity, security/disposition clarification, minimal representation of existing backend response/protocol contracts, authoritative WebSocket wire versioning, and the typed-client architecture/generator foundation. It does **not** complete Feature 53.
* **Prompt 1B — Generated Contracts and Typed Clients** consumes approved 1A contracts, generates complete client ownership and the schema registry, completes Feature 53, and runs the final Prompt-1 gates.

The split is mandatory because generated frontend metadata cannot authoritatively resolve API compatibility ownership, Access security policy, or wire-protocol compatibility. Prompt 2 requires successful Prompt 1B completion, not merely completion of the architecture subgate.

| Prompt | Scope | Prerequisites | Feature IDs | Deliverables and observable acceptance | Verification | Rollout and rollback |
|---:|---|---|---|---|---|---|
| 0 | **re-baseline and reconciliation** | Exact-HEAD Prompt 0 B0 gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 1 | **generated contracts, schema registry and verified clients** | Prompt 0 acceptance and all inherited security gates | 53 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 2 | **adapters, view models, provenance and request lifecycle** | Prompt 1 acceptance and all inherited security gates | 52,54 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 3 | **Proof-of-Access frontend security shell** | Prompt 2 acceptance and all inherited security gates | 67 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 4 | **WebSocket lifecycle, fixtures and degraded/fallback laboratory** | Prompt 3 acceptance and all inherited security gates | 59,60 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 5 | **routes, navigation, flags and rollback controls** | Prompt 4 acceptance and all inherited security gates | 55,56,58 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 6 | **theme, typography, Liquid Glass, geometry and visual performance** | Prompt 5 acceptance and all inherited security gates | 01,02,03,04,05,06,07,08,09,11,12,50,51 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 7 | **responsive shell, command palette, mobile actions and Wow migration** | Prompt 6 acceptance and all inherited security gates | 10,37,38,48,57 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 8 | **Overview and Operations health/providers/storage** | Prompt 7 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 9 | **Operations incidents/SLO/jobs and Market overview/signals** | Prompt 8 acceptance and all inherited security gates | 30,31,32,33,35,45,46 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 10 | **Market timeline/replay/attribution/narratives/evidence/sources** | Prompt 9 acceptance and all inherited security gates | 13,14,15,16,17,19,21,22 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 11 | **similarity/uncertainty/chart accessibility/export** | Prompt 10 acceptance and all inherited security gates | 18,20,47 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 12 | **Trace submit and dynamic report** | Prompt 11 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 13 | **Trace topology/time travel/disagreement/privacy** | Prompt 12 acceptance and all inherited security gates | 23,24,25,26 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 14 | **Trace proof-packet and Evidence list/detail/drawer** | Prompt 13 acceptance and all inherited security gates | 43 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 15 | **Evidence chain/replay/lineage/verification/copy/export** | Prompt 14 acceptance and all inherited security gates | 27,28,29,44,62 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 16 | **Access checkout/issuance/challenge/PoP session** | Prompt 15 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 17 | **Access profile/limits/entitlements/delegation** | Prompt 16 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 18 | **Access recovery/lockdown/revocation/session expiry** | Prompt 17 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 19 | **Console Policy/Audit/signal/Treasury review** | Prompt 18 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 20 | **entities/watchlists/fees/on-chain/Citadel/Sovereign Grid** | Prompt 19 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 21 | **plugins/webhooks/API Explorer** | Prompt 20 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 22 | **LNURL and separate PayRegister** | Prompt 21 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 23 | **cross-screen state/privacy/accessibility/EN-RU/timezone/units** | Prompt 22 acceptance and all inherited security gates | 34,36,39,40,41,42,61,63,64,65,66 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 24 | **observability/performance/CSP/dependencies/security** | Prompt 23 acceptance and all inherited security gates | 49,68,69 | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
| 25 | **regression/cleanup/docs/release evidence/rollback rehearsal** | Prompt 24 acceptance and all inherited security gates | None (mandatory domain work) | Own only the named workflows; deterministic artifacts/code, complete required states, privacy and accessibility behavior, and rollback notes. Accept only on current contract → security → client → adapter/view model → trigger → named render → states → privacy → contract test → browser evidence. | Focused unit/contract checks plus browser network/DOM/a11y at desktop/mobile where UI exists; failures are blockers, mocks are not runtime proof. | Per-surface/domain flag, staged local-first enablement; disable flag/revert this stage while retaining backend contracts and prior verified stage. |
