# Reflex Frontend

This directory is intended to house the Reflex-based web interface.

The current implementation of the Reflex frontend lives in the `reflex_frontend/` directory at the root of the repository. Once the migration is complete, the contents of that directory should be moved here. For now, developers can continue to work within `reflex_frontend/` while planning the transition.
