export type PublicStatus={platform_status:string;production_calibrated:boolean;known_limitations:string[]}
export type PublicTraceSummary={report_id:number;band:string;risk_summary:string;safety_warnings:string[];created_at?:string}
