import React from 'react'
export function NoCustodyNotice(){return <p className='text-sm'>No-custody guarantee: frontend does not handle seed phrases/private keys and does not sign or broadcast transactions.</p>}
