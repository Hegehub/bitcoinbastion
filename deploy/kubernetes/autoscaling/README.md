# Autoscaling

- API HPA is standard Kubernetes autoscaling.
- Worker KEDA ScaledObject is optional and requires KEDA installation in-cluster.
- Use staging load tests and evidence before enabling in production.
