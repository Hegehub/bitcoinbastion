apiVersion: batch/v1
kind: Job
metadata:
  name: bitcoin-bastion-database-restore-drill
  namespace: bitcoin-bastion
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: restore-drill
          image: postgres:16
          command: ["/bin/sh","-c","echo manual restore drill with explicit confirmation only"]
