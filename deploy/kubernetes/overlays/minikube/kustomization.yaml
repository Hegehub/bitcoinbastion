apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bitcoin-bastion-minikube

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: patch-env.yaml
  - path: patch-ingress.yaml
