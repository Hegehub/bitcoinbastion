apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitcoin-bastion-api
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: api
          image: ghcr.io/your-org/bitcoin-bastion@sha256:replace-with-approved-digest
          resources: {requests: {cpu: 500m, memory: 1Gi}, limits: {cpu: "1", memory: "2Gi"}}
          env:
            - name: ENVIRONMENT
              value: "production"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitcoin-bastion-worker
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: worker
          image: ghcr.io/your-org/bitcoin-bastion@sha256:replace-with-approved-digest
          resources: {requests: {cpu: 500m, memory: 1Gi}, limits: {cpu: "1", memory: "2Gi"}}
          env:
            - name: ENVIRONMENT
              value: "production"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitcoin-bastion-beat
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: beat
          image: ghcr.io/your-org/bitcoin-bastion@sha256:replace-with-approved-digest
          resources: {requests: {cpu: 500m, memory: 1Gi}, limits: {cpu: "1", memory: "2Gi"}}
          env:
            - name: ENVIRONMENT
              value: "production"

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: {name: bitcoin-bastion}
spec:
  rules:
    - host: bitcoin-bastion-prod.example.com
