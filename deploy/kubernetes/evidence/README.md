# Evidence jobs

- `evidence-verify-job.yaml`: verifies required artifacts exist.
- `evidence-archive-job.yaml`: packages evidence into a tarball and prints checksum.
- Optional upload can be wired by setting `EVIDENCE_ARCHIVE_UPLOAD_URL`; disabled by default.
