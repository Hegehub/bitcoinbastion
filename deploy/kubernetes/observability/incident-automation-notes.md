# Incident automation notes
Automation hooks create incident records and attach diagnostics; no automatic rollback/destructive action by default.
Include latest deployment SHA, observability snapshot, provider summary, evidence status, and rollback instructions in notifications.
