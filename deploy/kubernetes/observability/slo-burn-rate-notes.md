# SLO burn-rate notes
Internal SLO alerts are operational only (not public SLA). Fast/slow burn windows are conservative defaults and require environment tuning.
Missing metrics should be tracked explicitly before enabling stricter SLO math.
