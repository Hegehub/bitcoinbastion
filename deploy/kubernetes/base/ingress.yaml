apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bitcoin-bastion
  namespace: bitcoin-bastion
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: bitcoin-bastion.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bitcoin-bastion-api
                port:
                  number: 8000
