apiVersion: v1
kind: Service
metadata:
  name: bitcoin-bastion-api
  namespace: bitcoin-bastion
  labels:
    app: bitcoin-bastion-api
spec:
  selector:
    app: bitcoin-bastion-api
  ports:
    - name: http
      port: 8000
      targetPort: http
