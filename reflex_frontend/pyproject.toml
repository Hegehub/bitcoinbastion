[project]
name = "bitcoin-bastion-reflex-frontend"
version = "0.1.0"
description = "Parallel Reflex frontend for Bitcoin Bastion"
requires-python = ">=3.12"
dependencies = [
    "reflex",
    "httpx",
    "pydantic",
    "pydantic-settings",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "mypy",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = []

[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["bastion_ui/tests"]
pythonpath = ["."]
addopts = "-q"
